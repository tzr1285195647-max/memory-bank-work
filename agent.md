# 记忆银行开发上下文与 Agent 实施规范

> 本文档是“记忆银行”项目的持续开发基线。后续开发者或 AI Agent 在修改代码前应先阅读本文，并以本文记录的产品目标、数据规则、阶段边界和验收标准为准。
>
> 最近更新：2026-09-16

## 1. 项目目标

“记忆银行”是一个面向家庭记忆记录与整理的微信小程序。核心演示流程是：

1. 长辈录制口述原声。
2. 腾讯云 ASR 将录音转成文字。
3. AI 清理口语、提取事实并根据缺失信息继续采访。
4. 家庭成员可以跨账号校对、确认和管理记忆碎片。
5. 用户勾选多段记忆碎片，由故事 Agent 生成完整故事。
6. 长辈人工确认后发布到家庭故事列表。
7. 后续将多个已确认故事整理成带插画和时间线的家庭回忆录，并导出 PDF。

初赛只需要在本机真机演示，不申请公网服务器和 HTTPS 域名。微信小程序连接同一局域网内的本机后端，业务数据库、录音文件和生成结果保存在本机。腾讯云 ASR 和真实大模型启用后，必要的录音或文本会发送给对应服务商，界面不得再宣称“所有内容完全不上传公网”。

## 2. 当前技术基线

- 客户端：原生微信小程序。
- 本机后端：FastAPI。
- 业务数据库：SQLite。
- Agent 编排：LangGraph。
- Checkpoint：SQLite Checkpointer，用于采访流程暂停、恢复和人工确认。
- 录音转写：腾讯云 ASR，当前已真实启用。
- 大模型适配：OpenAI 兼容接口，配置支持 DeepSeek 等模型。
- 当前真实运行状态：`AGENT_MODE` 尚未切换为 `llm` 时会使用 `mock-agents`。填写密钥不等于已经启用真实模型；开发完成后必须通过 `/api/agent/status` 验证 `llmEnabled=true`。
- 前端配置 `fallbackToMock=false`，多账号演示不得静默混入本地假数据。

任何密钥只能保存在被 Git 忽略的 `.env` 中，不得写入本文、源码、测试快照或提交到 GitHub。

## 3. 固定演示账号与家庭关系

本机预置四个家庭账号：

| 账号 | 身份 | 说明 |
| --- | --- | --- |
| 林奶奶 | 长辈 | 主要故事讲述者 |
| 王爷爷 | 长辈 | 第二位故事讲述者 |
| 小刘 | 晚辈家属、家庭管理员 | 邀请成员、协助校对和整理 |
| 小李 | 晚辈家属 | 协助校对和整理 |

四个账号属于同一家庭。故事、录音、碎片、建议和操作记录以 `familyId` 隔离并在家庭内共享。

### 3.1 不可破坏的归属规则

- 录音创建时确定 `narratorUserId`，以后不得因为校对人、生成故事的人或当前登录账号改变。
- 小刘校对林奶奶的录音后，仍然必须显示“林奶奶的记忆”，可以另行显示“小刘已确认”。
- 一次故事生成只能选择同一位讲述者的碎片；混选林奶奶和王爷爷的碎片时必须阻止并提示重新选择。
- 故事中的讲述者来自录音/碎片证据，不信任客户端临时传入的姓名。
- 家属可以校对碎片、生成故事和提交建议；故事最终确认与发布由长辈完成。

## 4. 核心 Agent 架构

Agent 的数量表示独立的智能职责，不表示需要购买多个模型或准备多个密钥。多个 Agent 可以共用同一个模型接口，通过不同 Prompt、输入输出 Schema 和 LangGraph 子图实现。

当前故事阶段共建设 5 个核心 Agent；家庭回忆录阶段再建设 2 个 Agent。

### 4.1 Agent 1：口述校对 Agent

输入：腾讯云 ASR 原始转写、讲述者、主题和必要上下文。

允许执行：

- 修正明显错别字和常见同音字。
- 删除无意义的“呃、嗯、那个”等语气词。
- 减少连续重复的“然后、后来、接着”。
- 补充标点、合理分段。
- 保留方言、人物称呼、口头习惯和原有事实。
- 对不确定的人名、地名、年代给出待确认标记。

禁止执行：

- 增加原文不存在的人物、时间、地点、动作、结果或情绪。
- 把模糊信息擅自改成确定事实。
- 为追求文学性补写情节。
- 覆盖或删除 ASR 原始文本。

必须同时保存：

1. `asrRawText`：ASR 原始文字。
2. `agentCleanText`：Agent 清理建议。
3. `confirmedText`：用户最终确认文字。

只有 `confirmedText` 可以成为记忆碎片和后续故事证据。

建议输出固定 JSON：

```json
{
  "cleanText": "清理后的文字",
  "changes": [
    {"type": "delete_filler", "before": "呃", "after": "", "reason": "无意义语气词"}
  ],
  "uncertainties": [
    {"text": "新建县", "reason": "地名识别不确定"}
  ]
}
```

### 4.2 Agent 2：记忆证据 Agent

输入：用户已确认的碎片文字和对应录音元数据。

职责：

- 提取时间、地点、人物、事件、结果、影响、感受七类要素。
- 每个事实保存 `fragmentId`、原文 `quote` 和讲述者。
- 识别不同碎片之间的时间、人物或事件冲突。
- 为采访提示、故事写作和事实审计提供结构化依据。

输出必须通过 Pydantic/JSON Schema 校验，并验证 `quote` 确实是确认文本的原文子串。不得保存无法追溯的事实。

### 4.3 Agent 3：采访提示 Agent

输入：当前主题、已确认碎片、已提取事实、已问问题和讲述者停止意愿。

职责：

- 分析七类要素还缺少什么。
- 一次只提出一个主要问题，最多附带一个相关小问题。
- 避免重复提问。
- 优先询问能显著补全故事的问题。
- 当资料充分时明确输出 `complete=true`。
- 识别“不想讲了、记不清了、先到这里”等停止表达并尊重停止。

结束条件：

- 故事基本要素完整；或
- 达到采访轮次上限；或
- 连续两轮没有获得新事实；或
- 讲述者主动停止；或
- Agent 判断材料已经足够形成完整故事。

### 4.4 Agent 4：故事写作 Agent

输入：用户勾选的同一讲述者记忆碎片、结构化事实、时间顺序、证据关系和所选风格。

三种风格：

- `raw / 原味口述`：保留表达习惯，只做标点、分段和少量重复删除。
- `natural / 自然整理`：调整顺序和过渡，使内容流畅，但不增加事实。
- `book / 适合成书`：形成更完整的叙事结构和文学表达，但每个事实仍须有证据。

输出至少包含标题、正文、讲述者、引用的 `fragmentIds`、句子级证据映射和风格。不得混入其他讲述者的内容。

### 4.5 Agent 5：事实审计 Agent

输入：故事正文、证据 Agent 结果和原始确认碎片。

逐句检查：

- 是否能找到原声/碎片依据。
- 是否新增人物、时间、地点、动作、结果或情绪。
- 是否改变人物关系或讲述者身份。
- 是否把模糊年代改成具体年份。
- 是否混入其他家庭成员的故事。
- 是否存在碎片冲突而未标出。

输出：通过句、待确认句、无依据句、冲突和修改建议。存在无依据事实时禁止发布，退回写作或要求继续讲述。

### 4.6 Agent 6：家庭回忆录编辑 Agent（第二阶段）

只读取已经由长辈确认发布的故事，负责：

- 按年代、人生阶段和家庭主题组织章节。
- 合并重复故事但保留不同讲述者视角。
- 生成序言、章节标题、章节导语和结语。
- 保留章节、故事、讲述者和证据来源。
- 生成回忆录结构 JSON，不直接生成 PDF。

### 4.7 Agent 7：美术编排 Agent（第二阶段）

职责是为每页输出受约束的版式方案和插画提示词，例如：

- 页面类型。
- 版式模板 ID。
- 视觉情绪、配色、装饰元素。
- 插画提示词。
- 文字与图片区域。

美术 Agent 只能从预设模板中选择，不得自由输出不可控页面代码。

## 5. 推荐 LangGraph 父图

```text
START
  -> load_context
  -> consent_gate
  -> record_and_asr
  -> transcript_clean_agent
  -> human_transcript_review (interrupt)
  -> evidence_agent
  -> interview_director_agent
       -> need_more -> human_record_more (interrupt) -> record_and_asr
       -> complete
  -> story_writer_agent
  -> evidence_audit_agent
       -> failed -> writer_revision / request_more
       -> passed
  -> elder_story_review (interrupt)
       -> edit -> evidence_audit_agent
       -> request_more -> interview_director_agent
       -> reject -> delete_draft
       -> approve -> publish_story
  -> END
```

每个节点输出必须可序列化并具有明确 Schema。外部调用、文件写入和数据库写入必须具备幂等性，避免 LangGraph 恢复时重复执行。

## 6. 人工确认点

系统必须保留三类 Human-in-the-loop：

1. 口述校对完成后，用户确认文字才能保存为记忆碎片。
2. 单篇故事完成后，长辈确认才能发布。
3. 家庭回忆录和 PDF 完成后，家庭管理员确认才能正式导出。

Agent 建议不得自动替代用户确认，不得把 Agent 生成内容伪装成原话。

## 7. 家庭邀请需求

短信验证码继续使用固定演示验证码 `246810`，不接短信服务。

邀请流程需改为：

```text
管理员小刘输入手机号
  -> 创建 pending 邀请
  -> 被邀请人注册或登录
  -> 显示家庭邀请
       -> 接受：加入家庭，状态 accepted
       -> 拒绝：不加入家庭，状态 rejected
```

不得再让已注册账号在未确认的情况下直接加入家庭。

本阶段暂不实现：撤销邀请、重发、过期、多家庭切换。

## 8. 家庭纪念册与 PDF

PDF 生成不是 Agent，本质是确定性工具链：

```text
已确认故事
  -> 回忆录编辑 Agent
  -> 回忆录结构 JSON
  -> 美术编排 Agent
  -> 插画生成模型/本地缓存插画
  -> 固定 HTML/CSS 模板渲染
  -> PDF 预览
  -> 人工确认
  -> 一键导出
```

建议至少准备：

- 3 种封面模板。
- 3 种正文模板。
- 2 种章节页模板。
- 4 种插画位置。
- 固定字体、字号、页边距、安全区域和页码规则。

Agent 负责选择模板和生成插画提示词；渲染程序负责分页、字体、图层、图片裁切和 PDF 文件。不得让大模型直接自由生成 PDF。

动态插画需要单独的图片生成接口。为提高现场稳定性，应支持预生成和本机缓存；已经生成的插画不得在每次打开预览时重复请求。

## 9. RAG 与记忆技术决策

当前阶段不建设完整 RAG。数据量较小时优先使用：

- SQLite 结构化过滤。
- `familyId`、`narratorUserId`、`topicId`、`storyId`、`memoryYear`、`lifeStage` 和 `fragmentId`。
- SQLite FTS5 关键词全文搜索。
- 结构化事实和证据关系。

当单个家庭积累约 50—100 段以上碎片，或需要跨故事语义查询、回忆录跨年代检索时，再增加混合检索：

```text
家庭/讲述者/时间过滤
  -> FTS5 关键词检索
  -> 向量语义检索
  -> 重排序
  -> 少量相关证据交给 Agent
```

RAG 只能检索已确认内容，必须先按 `familyId` 隔离，防止跨家庭数据泄漏。

概念边界：

- Checkpoint：保存一次 Agent 工作流执行到哪一步。
- 长期记忆 Store：保存跨会话的家庭人物、关系、地点和偏好。
- SQLite 业务库：保存账号、邀请、录音、碎片、故事和审核记录。
- RAG：从大量已确认材料中召回与当前任务相关的少量证据。

## 10. Agent 工程要求

### 10.1 结构化输出

- 所有 Agent 输出必须使用 JSON Schema/Pydantic 校验。
- Schema 校验失败应重试一次；仍失败则进入安全降级并记录原因。
- 任何引用的 `fragmentId`、`recordingId`、`storyId` 都必须存在且属于当前家庭。

### 10.2 模型分层

- 校对、结构化提取可以使用速度快、成本低的模型。
- 采访导演、故事写作、回忆录编辑优先使用能力较强的模型。
- 插画使用独立图片生成模型。
- 不需要为每个 Agent 使用不同供应商，也不需要多个 API Key。

### 10.3 可观测性

每次 Agent 调用至少记录：

- Agent 名称和版本。
- 模型名称。
- Prompt 版本。
- 输入的碎片/故事 ID。
- 结构化输出。
- 耗时、重试次数。
- 是否发生 Mock/规则降级。
- 错误原因。
- 用户最终接受、修改或拒绝。

界面应能清楚区分：真实模型、规则降级、人工编辑，不能将降级结果伪装成真实 AI 结果。

### 10.4 评测集

至少建立以下自动化用例：

- 不得把“小刘校对”改成“小刘讲述”。
- 林奶奶和王爷爷的碎片不能混合生成同一篇单人故事。
- 校对 Agent 不得新增原文事实。
- 无证据句子不能通过审计。
- 采访 Agent 不重复提问并能识别停止意愿。
- 三种写作风格有明显差异但事实一致。
- 模型输出非法 JSON 时流程可安全恢复。
- 模型超时或额度不足时已保存碎片不能丢失。

## 11. 已实现能力

- 注册和密码登录。
- 四个本机演示账号与同一家庭协作。
- 年龄、性别自动头像。
- 冷启动强制登录。
- 家庭成员查询、邀请、修改、移除。
- 微信录音和腾讯云 ASR。
- 转写文字编辑、确认和保存。
- 多段记忆碎片的勾选、编辑、删除、排序和移动。
- 讲述者标签和校对人记录。
- 三种故事整理风格入口。
- 故事时间线、列表、搜索、编辑和删除。
- 家庭建议与长辈处理。
- 授权撤回、内容删除和操作审计。

## 12. 当前优先级

### P0：立即完成

1. 启用真实 LLM：配置 `AGENT_MODE=llm`，重启后端并验证 `/api/agent/status`。
2. 实现口述校对 Agent，并保留原始/建议/确认三份文本。
3. 完善记忆证据 Agent 的结构化事实与引用。
4. 将采访提示 Agent 切换为真实模型并实现明确结束判断。
5. 将故事写作 Agent 切换为真实模型。
6. 完善逐句事实审计 Agent。
7. 建立完整 Agent 调用日志和评测集。
8. 修复碎片生成故事后错误显示“继续恢复采访”的逻辑；碎片故事应开启新的补充会话或隐藏恢复按钮。
9. 删除转写失败后的“填入演示文字”，只保留重试和人工填写。

### P1：核心协作完善

1. 家庭邀请增加接受/拒绝。
2. 修正隐私文案，准确说明腾讯云和模型调用的数据范围。
3. 家庭看板“待确认”改为“待长辈确认”或“待家庭协作”。
4. 隐藏开发用组件预览页。
5. 在正式纪念册完成前隐藏或明确标记半成品入口。

### P2：家庭回忆录

1. 回忆录编辑 Agent。
2. 美术编排 Agent。
3. 插画生成与本机缓存。
4. HTML/CSS 模板排版。
5. PDF 预览和一键导出。
6. 数据量足够后再加入混合 RAG。

## 13. 明确暂缓项

以下不是当前缺陷，不得擅自扩展：

- 真实短信发送服务；继续使用固定验证码。
- 账号找回。
- 手机号和密码编辑。
- 上传头像。
- 故事可见范围；当前默认家庭成员可见。
- 多家庭创建和切换。
- 邀请撤销、重发和过期。
- 创意交付和权利安全检查。
- 公网服务器、域名和正式部署。

## 14. UI 与交互硬约束

- 所有固定底部栏页面必须为内容保留安全区和底部间距，任何按钮不得被底栏遮挡。
- 主 Tab 页面不显示容易误导的返回按钮；返回按钮仅用于二级页面。
- 导航必须有点击锁，避免一次点击重复跳转两次。
- 录音、上传、转写、生成和保存都必须有清晰的进行中、成功、失败和重试状态。
- 页面不得因后端失败静默填入虚构文本。
- 列表必须显示讲述者标签；涉及校对时同时显示确认人。
- 长耗时 Agent 调用要允许用户看到进度，但不得阻塞录音和已保存碎片。

## 15. 开发与验收命令

修改完成后至少运行：

```powershell
npm run test:miniprogram
.\.venv\Scripts\python.exe -m pytest backend\tests -q
.\.venv\Scripts\python.exe backend\demo_check.py --readonly
```

真实模型阶段还必须运行真实 HTTP 检查，并确认：

- `provider` 不再是 `mock-agents`。
- `configuredMode` 为 `llm`。
- `llmEnabled` 为 `true`。
- `fallbackCount` 没有在正常演示中持续增加。
- 输出中的讲述者、证据和碎片 ID 正确。

## 16. 标准演示场景

完整验收使用以下流程：

1. 林奶奶登录，选择“上学的日子”。
2. 分三次录制同一个故事。
3. 每次录音由腾讯云转写，再由校对 Agent 清理。
4. 林奶奶或小刘人工确认文字，保存为三段“林奶奶的记忆”。
5. 采访提示 Agent 根据已有碎片提出下一问题，并在材料充分时停止。
6. 小刘登录，检查三段碎片，编辑明显错误并留下“小刘已确认”。
7. 小刘勾选三段碎片，选择“适合成书”，生成完整故事。
8. 事实审计 Agent 显示句子与碎片证据关系。
9. 林奶奶登录，处理家庭建议并最终确认发布。
10. 故事列表显示“林奶奶的记忆”，不得显示为“小刘亲口讲述”。
11. 后续回忆录阶段将该故事按时间线收录、生成插画预览并导出 PDF。

## 17. 完成定义

一个 Agent 功能只有同时满足以下条件才算完成：

- 调用了真实模型而不是仅有页面外壳或规则 Mock。
- 输入输出 Schema 明确且经过验证。
- 数据权限和家庭隔离正确。
- 讲述者与校对者不会混淆。
- 所有生成事实可以追溯到确认碎片。
- 失败时不丢失录音、碎片和人工修改。
- 有自动化测试和至少一个完整真机演示用例。
- 界面明确显示运行状态、错误和降级情况。
- 人工确认点真实生效。

后续开发如需改变上述架构或业务边界，应先更新本文，再修改实现，避免代码与产品规则再次分离。
